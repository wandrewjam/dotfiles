- shell:
    - ./install-omz.sh

- defaults:
    link:
        relink: true

- link:
    ~/.zshrc:
        force: true
        path: .zshrc
    ~/.vimrc: .vimrc
    ~/.vim: .vim
    ~/.gitignore_global: .gitignore_global
    ~/.emacs.d/init.el: .emacs.d/init.el
