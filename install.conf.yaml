- shell:
    - ./install-omz.sh

- defaults:
    link:
        relink: true

- link:
    ~/.zshrc:
        force: true
        path: .zshrc
    ~/.vimrc: .vimrc
    ~/.vim: .vim
    # ~/.emacs.d: .emacs.d
